# 1. Prompt



## Take1

```sh
pdf 파일을 rag 학습 시켜서 해당 내용에 대한 답변을 받을 수 있도록 하는 Backend 프로그램을 개발하고 싶어.

필요한 기능은 아래와 같아.

1. FastAPI 기반의 App으로 전환해줘.
   - chat 기능을 담당하는 frontend 에서 부터 api 호출로 이어지는 backend 로 수행될꺼야.

2. 기본 uri는 ~/knowledgeops/api 로 해줘.

3. FAISS_INDEX_PATH 위치가 비워져 있으면 항상 벡터 인덱스를 새로 생성해야 해.(지금 있는 로직 그대로 사용)
   - 그리고 강제로 벡터 인덱스 재 생성도 수행할 수 있어야 해.
   - 특정 uri 를 호출해서 인덱스 재 생성할 수 있도록 해줘.

4. 질문은 api 호출로 받아서 응답할 수 있어야 해.
   - 그러므로 소스내에 질문은 없어도 됨.




```

